This is an attempt at separating claude stuff from other code.

For now, we put the claude stuff/helpers in a separate worktree (branch):

- CLAUDE.md
- Makefile
- README.md (this)

Checkout using:

    git worktree add -b x-agent ../M5Cardputer-UserDemo-agent
